import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.agendaclayton.app',
  appName: 'AgendaClayton',
  webDir: 'dist/agenda-clayton/browser'
};

export default config;
